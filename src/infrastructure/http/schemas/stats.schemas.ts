export const alertsBySeveritySchema = {
  tags: ['Stats'],
};

export const notificationRateSchema = {
  tags: ['Stats'],
};

export const resolutionTimeSchema = {
  tags: ['Stats'],
};

export const topActiveUsersSchema = {
  tags: ['Stats'],
};
